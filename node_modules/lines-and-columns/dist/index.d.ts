export declare interface SourceLocation {
    line: number;
    column: number;
}
export default class LinesAndColumns {
    private string;
    private offsets;
    constructor(string: string);
    public locationForIndex(index: number): SourceLocation | null;
    public indexForLocation(location: SourceLocation): number | null;
    private lengthOfLine(line);
}
